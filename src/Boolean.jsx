function Boolean(){
    let res=true;
   return (
    //conditional rendering
    //conditional statement
    
            res === true ? (
            <div>
                it is true
            </div>
            ) : (
                <h3>it is false</h3>
            )
    
   );
}
export default Boolean;